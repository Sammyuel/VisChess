'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _cliEngineHeroku = require('cli-engine-heroku');

class Index extends _cliEngineHeroku.Command {

  async run() {
    let { body: account } = await this.heroku.get('/account');
    if (account.two_factor_authentication) {
      this.out.log('Two-factor authentication is enabled');
    } else {
      this.out.log('Two-factor authentication is not enabled');
    }
  }
}
exports.default = Index;
Index.topic = 'auth';
Index.command = '2fa';
Index.description = 'check 2fa status';
Index.aliases = ['2fa', 'twofactor'];